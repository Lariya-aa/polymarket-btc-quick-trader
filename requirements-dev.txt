# Dev-only dependencies. Pure-function tests don't import the runtime
# deps (no aiohttp, no py_clob_client_v2), so this file is enough to
# `pytest tests/` on a fresh machine.
-r requirements.txt
pytest>=8.0,<9.0

# Packaging — only needed when running packaging/build_*.sh|.ps1.
pyinstaller>=6.0,<7.0
